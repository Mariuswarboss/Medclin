<UserControl x:Class="Mediclin.UI.Views.Shared.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel LastChildFill="False" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="Notificari" Style="{StaticResource HeadingText}" />
                <TextBlock Text="{Binding UnreadSummary}" Style="{StaticResource MutedText}" Margin="0,6,0,0" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Refresh"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding RefreshCommand}"
                        Margin="0,0,8,0" />
                <Button Content="Marcheaza toate ca citite"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding MarkAllReadCommand}" />
            </StackPanel>
        </DockPanel>

        <ProgressBar Grid.Row="1"
                     Height="4"
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                     Margin="0,0,0,12" />

        <Grid Grid.Row="2">
            <Border Style="{StaticResource CardBorder}"
                    Padding="28"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel Width="360">
                    <TextBlock Text="N" FontSize="34" FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource PrimaryGreenBrush}" />
                    <TextBlock Text="Nu ai notificari in acest moment"
                               FontWeight="SemiBold"
                               FontSize="18"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,4" />
                    <TextBlock Text="Cand apar programari, mesaje, analize sau actualizari de consultatie, le vezi aici."
                               Style="{StaticResource MutedText}"
                               TextWrapping="Wrap"
                               TextAlignment="Center" />
                </StackPanel>
            </Border>

            <ScrollViewer VerticalScrollBarVisibility="Hidden">
                <ItemsControl ItemsSource="{Binding Notifications}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,10" Padding="16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Border Width="44" Height="44" CornerRadius="16" Margin="0,0,14,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{DynamicResource NeutralPillBgBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsUnread}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource PrimaryLightBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Tip, Converter={StaticResource StringToUpperConverter}}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource PrimaryGreenDarkBrush}" />
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Titlu}"
                                                       FontWeight="SemiBold"
                                                       FontSize="15"
                                                       Foreground="{DynamicResource TextPrimaryBrush}" />
                                            <Border CornerRadius="12" Padding="8,3" Margin="10,0,0,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{DynamicResource NeutralPillBgBrush}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsUnread}" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource WarningBgBrush}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding StatusText}" FontSize="11" FontWeight="SemiBold" />
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Mesaj}"
                                                   TextWrapping="Wrap"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Margin="0,6,0,0" />
                                        <TextBlock Text="{Binding CreatedText}"
                                                   Style="{StaticResource MutedText}"
                                                   Margin="0,8,0,0" />
                                    </StackPanel>

                                    <Button Grid.Column="2"
                                            Content="Marcheaza citita"
                                            Style="{StaticResource SecondaryButton}"
                                            Command="{Binding DataContext.MarkReadCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Visibility="{Binding IsUnread, Converter={StaticResource BoolToVisibilityConverter}}"
                                            Margin="16,0,0,0"
                                            VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <TextBlock Grid.Row="3"
                   Text="{Binding StatusMessage}"
                   Style="{StaticResource MutedText}"
                   Margin="0,12,0,0" />
    </Grid>
</UserControl>
