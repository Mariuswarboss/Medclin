<Window x:Class="Mediclin.UI.Views.Dialogs.ConfirmDeleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmare ștergere" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="{DynamicResource BackgroundPrimaryBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource ErrorRedBrush}">
            <TextBlock x:Name="TitleStrip" Text="Confirmare" Foreground="White" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold" />
        </Border>
        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock x:Name="MessageBlock" TextWrapping="Wrap" Margin="0,0,0,18" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Renunță" Style="{StaticResource SecondaryButton}" Click="Cancel_Click" Margin="0,0,8,0" />
                <Button Content="Șterge" Style="{StaticResource DangerButton}" Click="Delete_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
