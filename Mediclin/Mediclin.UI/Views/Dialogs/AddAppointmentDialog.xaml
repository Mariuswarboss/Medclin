<Window x:Class="Mediclin.UI.Views.Dialogs.AddAppointmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Programare" Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#0F6E56">
            <TextBlock Text="Programare nouă" Foreground="White" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold" />
        </Border>
        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock TextWrapping="Wrap" Style="{StaticResource MutedText}">
                Pentru o programare completă (pacient, interval, tip, motiv) folosiți formularul din panoul din dreapta
                pe pagina «Programări», după ce selectați ziua în calendar.
            </TextBlock>
            <Button Content="Înțeles" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" Margin="0,18,0,0"
                    Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
