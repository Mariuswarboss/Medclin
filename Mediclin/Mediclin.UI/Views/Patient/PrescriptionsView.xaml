<UserControl x:Class="Mediclin.UI.Views.Patient.PrescriptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Padding="24" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <DockPanel LastChildFill="False">
                <TextBlock Text="Retete active" Style="{StaticResource HeadingText}" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Reincarca" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" Margin="0,0,8,0" />
                    <Button Content="Solicita reinnoire" Style="{StaticResource PrimaryButton}" Command="{Binding RequestRenewalCommand}" />
                </StackPanel>
            </DockPanel>

            <Border Background="{StaticResource WarningBgBrush}" CornerRadius="8" Padding="14" Margin="0,12,0,16">
                <TextBlock Text="Retetele emise de medic apar automat aici dupa ce sunt salvate pe consultatie." />
            </Border>

            <ProgressBar IsIndeterminate="True"
                         Height="4"
                         Margin="0,0,0,14"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />

            <ItemsControl ItemsSource="{Binding Retete}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorder}" Margin="0,0,12,12">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding MedicamenteText}" Style="{StaticResource SectionTitleText}" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding MedicNume, StringFormat=Medic: {0}}" Style="{StaticResource MutedText}" />
                                    </StackPanel>
                                    <Border Grid.Column="1" Background="#EAF4F0" CornerRadius="14" Padding="10,6">
                                        <TextBlock Text="{Binding Status}" Foreground="{StaticResource PrimaryGreenDarkBrush}" FontWeight="SemiBold" />
                                    </Border>
                                </Grid>

                                <Grid Margin="0,12,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Emisa" Style="{StaticResource FieldLabel}" />
                                        <TextBlock Text="{Binding DataText}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="Expira" Style="{StaticResource FieldLabel}" />
                                        <TextBlock Text="{Binding ExpiraText}" />
                                    </StackPanel>
                                </Grid>

                                <ItemsControl ItemsSource="{Binding Medicamente}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#F8FAFC"
                                                    BorderBrush="{StaticResource BorderBrushNeutral}"
                                                    BorderThickness="1"
                                                    CornerRadius="14"
                                                    Padding="12"
                                                    Margin="0,0,0,8">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding NumeScurt}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                    <WrapPanel Margin="0,6,0,0">
                                                        <TextBlock Text="{Binding CantitateText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,12,0" />
                                                        <TextBlock Text="{Binding DozajText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,12,0" />
                                                        <TextBlock Text="{Binding FrecventaText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,12,0" />
                                                        <TextBlock Text="{Binding DurataText}" Foreground="{StaticResource TextSecondaryBrush}" />
                                                    </WrapPanel>
                                                    <TextBlock Text="{Binding Instructiuni}"
                                                               Foreground="{StaticResource TextTertiaryBrush}"
                                                               TextWrapping="Wrap"
                                                               Margin="0,6,0,0"
                                                               Visibility="{Binding Instructiuni, Converter={StaticResource NullToVisibilityConverter}}" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <TextBlock Text="{Binding Observatii}"
                                           Style="{StaticResource MutedText}"
                                           TextWrapping="Wrap"
                                           Visibility="{Binding Observatii, Converter={StaticResource NullToVisibilityConverter}}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="{Binding StatusMessage}"
                       Style="{StaticResource MutedText}"
                       Margin="0,6,0,0"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
